// import { Session } from "next-auth";
// import { createContext, useContext } from "react";

// const ApplicationGlobalContext = createContext<{
//   session: Session | null;
// } | null>(null);
// export const useApplicationGlobalContext = () => {
//   const context = useContext(ApplicationGlobalContext);

//   if (!context) {
//     throw new Error(
//       "useApplicationGlobalContext must be used within a ProductCardProvider"
//     );
//   }
//   return context;
// };

// export default ApplicationGlobalContext;
